services:
  paytrack:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "${PORT:-3000}:3000"
    env_file:
      - path: .env.docker
        required: false
    restart: unless-stopped

